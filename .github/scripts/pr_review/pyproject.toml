[project]
name = "pr-review"
version = "0.1.0"
description = "Provider-neutral GitHub pull request review MCP server"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["mcp[cli]>=1.26.0,<2"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
